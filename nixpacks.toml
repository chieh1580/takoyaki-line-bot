[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "PYTHONUNBUFFERED=1 python3 -m gunicorn app:app --bind 0.0.0.0:$PORT --access-logfile -"
